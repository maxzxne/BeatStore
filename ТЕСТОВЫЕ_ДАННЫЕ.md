# Тестовые данные для авторизации

## Администратор

**Логин:** `admin`  
**Пароль:** `admin123`  
**Email:** `admin@XWinner.beats.please.com`  
**Доступ:** Админ-панель

---

## Обычный пользователь

**Логин:** `test_user`  
**Пароль:** `test123`  
**Email:** `test@example.com`  
**Доступ:** Обычный пользователь (с тестовыми покупками)

---

## Примечания

- Администратор создается автоматически при первом запуске приложения (функция `create_admin_user()` в `backend/main.py`)
- Тестовый пользователь создается при заполнении базы данных тестовыми данными (функция `seed_test_data()` в `backend/seed_test_data.py`)
- Оба пользователя создаются автоматически, если их еще нет в базе данных


