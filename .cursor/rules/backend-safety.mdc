---
description: Do not change BeatStore backend contracts unless the shipped UI truly cannot work without it
globs: backend/**/*.py
alwaysApply: false
---

# Backend safety

Shipped product = **V2 frontend**. Backend трогать только если без этого нельзя собрать/починить UI или бизнес-флоу. V3 redesign не является причиной менять API.

Осторожно: API contracts, schema, payments, JWT, purchase/download, file upload.

Сейчас факты:

- Лицензии — это `purchase_type`: `mp3` | `wav` | `exclusive`, не отдельная сущность License.
- Поиск в каталоге частично клиентский.
- Оплата: PaymentIntent + Robokassa ResultURL. Клиентский `payment_success` для платных больше не принимается.
- Royalty/producers в моделях нет.

Если изменение нужно:

1. Зачем для shipped V2 / текущего бага
2. Что сломается
3. Минимальный diff
4. Проверить login, catalog, cart, purchase, download

Не добавлять поля «для красоты». Waveform peaks лучше считать на клиенте из demo, чем менять схему вслепую.
